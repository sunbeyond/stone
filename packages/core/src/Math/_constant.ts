export const nativeMath = Math;

export const nativeAbs = nativeMath.abs;

export const nativeCeil = nativeMath.ceil;

export const nativeFloor = nativeMath.floor;

export const nativeMax = nativeMath.max;

export const nativeMin = nativeMath.min;

export const nativeRandom = nativeMath.random;

export const nativeRound = nativeMath.round;

export const nativePow = nativeMath.pow;

export const PI2: number = nativeMath.PI * 2;

//  PI * 0.5
export const TAU: number = nativeMath.PI * 0.5;

// epsilon value
export const EPSILON: number = 1.0e-6;

// Convert degrees to radians.
export const DEG_TO_RAD: number = nativeMath.PI / 180;

// Convert radians to degrees.
export const RAD_TO_DEG: number = 180 / nativeMath.PI;
